export const hassUrl = (state, toAdd) => {
  state.hassUrl = toAdd
}

export const buttonWidth = (state, toAdd) => {
  state.buttonWidth = toAdd
}
export const buttonHeight = (state, toAdd) => {
  state.buttonHeight = toAdd
}
export const backgroundImage = (state, toAdd) => {
  state.backgroundImage = toAdd
}
export const screens = (state, toAdd) => {
  state.screens = toAdd
}
export const configEnabled = (state, toAdd) => {
  state.configEnabled = toAdd
}

export const configName = (state, toAdd) => {
  state.configName = toAdd
}

export const componentsToDisplay = (state, toAdd) => {
  state.componentsToDisplay = toAdd
}

export const tempSensor = (state, toAdd) => {
  state.tempSensor = toAdd
}

export const currentConditions = (state, toAdd) => {
  state.currentConditions = toAdd
}
export const homeScreenDelay = (state, toAdd) => {
  state.homeScreenDelay = toAdd
}

export const rotateBackground = (state, toAdd) => {
  state.rotateBackground = toAdd
}
export const rotateBackgroundDelay = (state, toAdd) => {
  state.rotateBackgroundDelay = toAdd
}
export const rotateBackGroundImages = (state, toAdd) => {
  state.rotateBackGroundImages = toAdd
}
